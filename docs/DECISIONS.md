# Decisions

Append-only. One line per decision, newest at the bottom. Thirty seconds to write.

**Why this file exists:** things reshape fast in a 24-hour build. This stops the 4 AM
argument about whether something was already settled, and it hands Lane D the raw
material for the README and the GTM brief.

**Format:** `HH:MM · what was decided — why`. If a decision reverses an earlier one, add
a new line, don't edit the old one. The trail matters.

---

## Locked before T-0

Carried in from `IDEA_SCOPE.md` §2. **Do not reopen during the build.**

| # | Decision | Why |
|---|---|---|
| 1 | Declared job is **produce the intake record + handoff**, never "triage the patient" | Triage has no obtainable ground truth in 24h; 12 checkable fields do. Caps at rubric L3 vs L5 |
| 2 | Primary track is **Revenue** | ICP is physically in the room; one real transaction is the cheapest L5 on the board |
| 3 | **US Twilio number** | An Indian DID needs a regulatory bundle, up to 3 business days. Impossible in-window |
| 4 | **Outbound is thin.** It exists to seed memory. Inbound is the hero | Outbound alone is a commodity; the loop between directions is the product |
| 5 | **No** acoustic distress detection · **no** diagnosis · **no** dispatch claim | Unverifiable across 3 cold runs, and each invites a challenge that cannot be won |
| 6 | Priority is **rule-cited**, and the UI renders the rule string | Converts a subjective judgment into an auditable one. Evidence for Memory L5 |
| 7 | Product name is **not Voxikin** — settled 30 Aug: **Voxikin** | Handbook rule 04. A distinct name keeps the build clear of the founder's company brand |
| 8 | Tool contract frozen at **T+1h**, not renegotiated | The only interface all four lanes share |

---

## Aug 29 — build night

| Time | Decision | Why |
|---|---|---|
| ~19:20 | Empty private repo `sahay` created on GitHub | `CC-1` — repo must not exist before T-0 (18:30). Timestamp is the proof |
| 20:23 | **Archive moved out of the project** to `~/Desktop/projects/sahay-archive/` — both `Archive 2/` and `Archive 2.zip` | It held a working codebase for the same use case *and* a `.env` with live Sarvam + Groq keys. Gitignoring is one `git add -f` away from shipping a disqualifier and two credentials into a public repo |
| 20:30 | Repo stays **private now, flip to public at submission** | A half-built repo shouldn't be public during judging prep; a credential accident shouldn't be permanently indexed |
| 20:30 | **Single `main` branch.** No per-lane branches | At hackathon pace, four branches cost more in merge friction than they save. Mitigated by directory ownership instead |
| 20:30 | **Empty stubs**, not working stubs | Faster to a landing spot for every lane; the critical-path files get filled first anyway |
| 20:30 | Generated artifacts (`.docx`, `.html`, `.zip`) are **gitignored** | The `.md` is the source of truth; binaries bloat diffs and go stale silently |
| 20:35 | **Pricing page + checkout → Lane D**, entirely | Three docs contradicted each other (`IDEA_SCOPE` §3 said C, `TRD` §1.2 said D, `TRD` §13 said C). It has zero API dependency and it is D's track proof |
| 20:35 | **Twilio purchase → D. Routing → A.** | Purchase is a card transaction; routing is config |
| 21:00 | **No AI attribution in commits**, PRs, or docs | Authorship is the team's to present. In a judged submission, tooling trailers in the history are a signal we don't want to send |
| 21:05 | **Notion owns checklist *state*. The repo owns checklist *content*.** State never flows back | Two-way sync would need the Notion API, conflict resolution, and a babysitter. The repo checklists also stay in git as context for anyone reading the code |
| 21:05 | **Directory ownership per lane**, enforced socially not by tooling | Git only fights you when two people write the same file. Four people on one branch is fine if nobody writes outside their directory |

---

## Aug 30

| Time | Decision | Why |
|---|---|---|
| — | **Product name settled: Voxikin.** Renamed across docs, checklists and both wireframe files (`sahay.app` → `voxikin.app`). Placeholder history: MediWatch → Sahay → *(briefly Voxikin)* → **Voxikin** | Closes locked decision 7 and `IDEA_SCOPE` §9 item 1. Voxikin is distinct from the founder's company brand, so `[V]` rule 04 (*"if your company builds in this space, you can't demo your existing product"*) is satisfied without a mentor ruling on brand reuse |
| — | **Caregiver auth built in-house, not on a hosted identity provider.** Own OTP endpoints in `api/auth/`, opaque session in an httpOnly cookie, Postgres-backed | Auth appears in neither PRD nor TRD — `FR-1`'s acceptance is only *"record exists with phone in E.164"* — so there was nothing to conform to. Owning it kept caregiver identity in the TRD's own `caregivers` table with no second source of truth, and it forced `api/` to become a real application (app factory, pool, schema), which every other lane needed anyway |
| — | **`/auth/*` and `/app/*` return a real 401; the seven tools keep `TRD §5.1`'s always-200 envelope** | The always-200 rule exists because *"a non-2xx makes the agent stall and the parent hears silence"* — that reasoning is about the voice agent, not a browser. A route guard has to tell "signed out" from "endpoint broke", and only a status code carries that. Business failures (wrong code, rate limited) keep the envelope. The T+1h contract freeze covers the seven tools only, so nothing here renegotiates it |
| — | **Phone OTP goes over WhatsApp before SMS** | India's TRAI requires DLT registration for A2P SMS — days to weeks, and the copy freezes once approved. WhatsApp is not SMS, so it can go live the same day, and `TRD §9` already puts WhatsApp first on the escalation ladder. SMS stays as the second rung for caregivers who do not use WhatsApp |
| — | Borderline note corrected back to `IDEA_SCOPE` §9 wording — **pre-product, no codebase**, never pitched or demoed; prior work is market research and a no-code voice agent | The README and the T-0 checklist had drifted into claiming "a prior codebase exists". The founder states there is none. ⚠️ **Not yet reconciled — see the note below.** |
| 02:37 | **`rx_extract` adopted as a first-class Voxikin module** (`api/rx_extract/`) — the prescription-reading pipeline from the team's own rx-extract research project, brought in as project code rather than an external dependency | The research repo has no remote, so a submodule was not possible; and the eval harness and dataset there are not wanted here. Only the pipeline came across. `normalize.py`, the validation rules, the HTTP surface and the tests were built for Voxikin. Documented in `api/rx_extract/README.md` |
| 02:41 | `api/rx_extract/normalize.py` **built for Voxikin** — expands the model's abstract `morning/afternoon/night` slots to clock times, anchored on the caregiver's own meal times rather than a fixed 08:00/14:00/20:00 | The app's `DraftMedicine.slots` needs `'HH:MM'` and the pipeline emits abstract slots, so nothing could consume the output without it. Anchoring on real meal times is the difference between a reminder at breakfast and one an hour before it |
| 02:44 | Lane B's `api/` opened for this work at the founder's explicit go-ahead; first real FastAPI app written in `api/main.py` (`/extract`, `/health`) | `api/` was 100% four-line stubs, so there was no backend to add a route to. Lane ownership waived deliberately, not by accident |
| 02:46 | A model refusal is its own outcome end to end: `VLMBlockedError` → `{kind:'blocked', needs_human_review:true}` → a distinct "not read" panel. It is never rendered as an empty schedule | "The safety filter blocked this" and "I read the page and there are no medicines" are different facts, and the second is a schedule someone might sign off. Collapsing them is the exact failure the pipeline was built to prevent |
| 02:48 | Prescription upload narrowed to **JPEG/PNG only**; PDF and HEIC now rejected at both the picker and the endpoint | `_detect_mime_type` silently falls back to `image/jpeg` for anything it does not recognise, so an accepted PDF would have been base64'd and handed to the model as a broken JPEG — a confident misreading of garbage |
| 02:50 | The fake upload progress bar on `1c` removed; the photo is now staged in memory and sent once, on `1d`, as part of the real extraction request | There is a real transfer now, and it happens on the next screen. An animated bar describing a network call that never happened was the one remaining lie in the flow |
| 03:05 | **Live-verified against ground truth**, 5 synthetic documents, `google:gemini-3.5-flash-lite`, ~2.5s and ~2.2k tokens each. All 5 came back `needs_review:false` at 0.95–0.98 confidence while 11 of 18 rows differed from the label and 5 carried an invented dose | This is the design doc's own S1 rationale reproduced live — *"the output is well-formed, confident, and wrong"*. Recorded as evidence that the mandatory sign-off gate is load-bearing, not ceremony. Note the numbers use strict equality and are **not** comparable to upstream's fuzzy-matched 0.980 composite |
| 03:08 | `normalize` now blanks a **bare unitless `dose_amount`** (`1`, `2`, `½`, `1/2`) when no strength was read, raises `strength_unknown`, and marks the row unclear. A dose_amount carrying a unit (`2 tsp`, `10 ml`) is kept | Where the line has no written strength the model lifts `"1"` off the leading digit of the `1-0-1` notation and reports it at 0.95 confidence with no flag of its own. Showing that as a dose presents an inferred value as a read one (S2). Verified live: the three affected documents now surface the rows instead of passing them |
| 03:09 | `strength_unknown` kept **out** of the `Flag` enum in `schema.py`; `NormalizedMedicine.flags` widened to `list[str]` | It is a normalization outcome, not something the extractor reported, so it does not belong in the model the extractor fills in |
| 03:22 | **Bug: `1d` hung on "still reading…" and never issued a request.** The `AbortController` in the effect's cleanup was removed; the StrictMode single-flight ref stays | StrictMode runs setup → cleanup → setup. Cleanup aborted the only run, then the second setup hit the `started` ref and declined to start another, so `fetch` rejected with `AbortError` before leaving the browser and the row froze at `reading`. Confirmed by the API log: zero browser-originated `POST /extract`. Not aborting costs little — a caregiver who navigates away mid-read finds the result waiting instead of paying for a second extraction |
| 03:24 | Every path out of `runExtraction` now resolves its row: a per-page `catch`, plus a `finally` that demotes any row still `pending`/`reading` to `failed` with a retry | The original failure was silent and terminal — no error, no retry, no way forward. A row stuck in `reading` should be structurally impossible, not merely fixed this once |
| 03:26 | ⚠️ **The app has no test setup**, so this class of bug is uncovered. The 33 Python tests exercise the pipeline, normalize and `/extract`; nothing exercises a React effect | Worth `vitest` + `@testing-library/react` if there is time — a StrictMode double-invoke test would have caught this before it reached a demo |
| 03:38 | **Bug: a second prescription showed the first one's medicines.** `ExtractionMeta` now records `source_files` (the `DraftFile` ids it was read from), and `1d` re-reads whenever the current files differ from those | The guard was `if (draft.ocrDone) return`, which persists in localStorage. It only says *a* prescription was read, not *which*, so a new photograph was suppressed and the previous reading rendered underneath it — looking read, and one tap from being signed off. The worst shape this bug could take |
| 03:40 | A new read now clears `medicines`/`extraction` before it starts, rather than after it succeeds | Otherwise the old prescription's rows sit under the new photograph while it reads, and survive as a signable schedule if the new read fails |
| 03:48 | **Extraction split onto its own base URL** (`EXTRACT_API_BASE`, `VITE_EXTRACT_API_BASE`) rather than sharing `API_BASE` | Pointing the single `API_BASE` switch at the extraction service 404'd every screen in the app: `hooks.ts` reads one flag to choose mock-or-live for *everything*, so enabling one real endpoint switched off the mock for the eight that do not exist yet. One switch cannot describe two backends with different readiness |
| 04:05 | **Care API built for real** — `api/schema.sql` (TRD §3 in SQLite), `api/db.py`, `api/routes_app.py`: the six `/app/*` reads, `/h/{token}`, and `POST /app/onboarding` | The onboarding draft was a dead end — extract a prescription, confirm it, and the calendar still showed fixture data, because nothing posted anywhere. SQLite so it runs from a clone with nothing to provision; column names and nullability are TRD §3 unchanged, so Postgres is a type substitution |
| 04:07 | The database **seeds from `scripts/mock-api.json`** (TRD §3.2) and only when empty | Seeding from the same fixture Lane C built every screen against means the live endpoints return shapes the app already renders, so mock → live is a base-URL change and not a debugging session. Seeding only on an empty database means a restart never overwrites a schedule someone signed off |
| 04:09 | **The FR-4 sign-off is enforced server-side**: `POST /app/onboarding` refuses an unsigned schedule, an empty one, or two priority medicines; `medications.confirmed_by`/`confirmed_at` are `NOT NULL` | A rule that lives only in a disabled button is not a rule — anything can POST. Design doc §10 is explicit that the confirmation fields are required rather than nullable-with-a-default, because a nullable `confirmed_by` is a gate that defaults to open |
| 04:12 | **The intro call is a first-class calendar event**, plus a banner while `intro_call_status` is `pending` | A schedule can be signed off and entirely dormant — no dose may be dialled until the parent has agreed on the intro call. If the calendar only drew doses, a caregiver would see a full week of reminders with no way to tell that none of them will ring |
| 04:14 | Extraction provenance now survives confirmation: `raw_line`, `confidence`, `extraction_flags`, `source_doc_id`, `excluded`/`exclusion_reason` are columns on `medications` | Closes SCHEMA-GAPS §7. Previously the evidence for every row was discarded the moment a caregiver signed off, leaving "Metformin 500mg at 08:30" with no record of what the paper said or how sure the model was |
| 04:32 | **Vitest + Testing Library added to `app/`**, with the suite rendering real screens rather than testing helpers | Two React lifecycle bugs reached a live screen past a fully green 55-test Python suite, because neither was reachable from a pure function. Both now have a regression test, and both were verified by reintroducing the bug and watching the tests fail — 4 failures for the StrictMode abort, 1 for the stale re-read guard. A test that passes on broken code is worth nothing |
| 04:34 | `clearFiles()` added and called once onboarding saves | Prescription photographs are held in memory for the length of the flow; keeping them past the point the schedule is stored is what DPDP guidance says not to do. It also gives the tests isolation, since the staged-image map is module scope |
| 04:35 | `@types/node` added as a dev dependency | `vite.config.ts` imports `node:url` and the repo had no node types, so `npm run typecheck` failed on a clean checkout. Unrelated to the test work but fixed while the build config was open |
| 04:55 | **`unknown` added as a fifth `DoseStatus`** — the degraded case where the agent could not reach the patient at all | It was previously unrepresentable, so a dose nobody could establish would have surfaced as `missed`, which asserts the dose was not taken. Same failure shape as reading a model refusal as "no medicines on the page": stating a fact nobody established. `missed` remains the only status that makes that claim |
| 04:57 | `escalations.dose_event_id` added, and the calendar names the alert beside an unreachable dose | Without the link "the escalation that fired" could only be inferred from timestamps, and a guess presented as a link is worse than no link at all |
| 04:58 | Of the three offered actions, only `Call {name} yourself` is live; `Try another number` and `Ask a neighbour` render disabled with the reason | Onboarding collects no second number and no neighbour (SCHEMA-GAPS §5). Consistent with how the escalation ladder already behaves — a control that looks dialable and silently does nothing is worse than one that says why it cannot |
| 05:00 | `db._day_shift()` now compares **local** dates, not UTC instants | At 03:00 IST the UTC date is still yesterday, so the fixture rebased a day backwards while `app/src/api/mock.ts` rebased against local midnight — mock and live disagreed about which day a dose belonged to |
| 05:20 | **`POST /app/medications`** — the medicine editor persists, with the doctor-advice attestation stored as **text** plus the diff, in `medication_changes` | Closes SCHEMA-GAPS §3 and the editor's `TODO(Lane B)`. The attestation is validated server-side because a rule enforced only by a disabled tickbox is skippable by anything that can POST, and a boolean against nothing is not an audit trail |
| 05:22 | Edits **update in place**; the schedule is not replaced wholesale | `raw_line`, `confidence` and `source_doc_id` belong to the reading a row came from and the editor never sees them. A wholesale replace would silently destroy the evidence for every medicine the caregiver did not touch |
| 05:24 | Stopping a medicine sets `medications.stopped_at` rather than deleting the row | `dose_events` reference medications, so a delete would either fail on the foreign key or take the history with it. What was already taken is still the record of what was taken |
| 05:26 | **`POST /app/doses`** — `Mark taken` writes a `dose_event`, and that write **is** the call cancellation | The scheduler dials slots with no `dose_events` row, so there is no separate "call cancelled" flag that could drift out of step with the record. `INSERT OR REPLACE` against the unique `(medication_id, slot_time)` index makes a double tap or a retry land on the same row (TRD §3.1) |
| 05:45 | **Day / Week / Month / Agenda** built on the calendar (frame `2f` header), all four reading the same `slotsForDay` expansion | They differ only in how much time is on screen — a day to act on, a week to check, a month to see a pattern, an agenda to read straight through. Sharing one expansion is what stops any two of them disagreeing about when a medicine is due |
| 05:47 | Range stepping moves by **one screenful of the current view**, not a fixed week, and the chip is labelled with what it will do | `‹ Previous week` in a day view is a lie about what the button does |
| 05:48 | `Print / share PDF` switches to Agenda and calls `window.print()`; `+ Add dose` routes to the medicine editor | The browser's print dialog is also the share sheet and the save-as-PDF on every platform this runs on, so there is nothing to build behind it — and a schedule on paper is the most useful thing this screen makes. Adding a dose is a schedule change, so it belongs in the editor rather than being a second way to write the same rows |
| 06:10 | **Drag-to-reschedule built as two distinct writes.** A plain drag moves one occurrence via `POST /app/doses/move`; a shift-drag changes `medications.slots` through the editor's own endpoint and moves every day | `medications.slots` are recurring and cannot say "just this Tuesday". One gesture covering two very different changes is only safe if the difference is visible, so the target cell reads *just this one* or *every day* before the drop, and the modifier is read at the drop rather than the pick-up |
| 06:12 | A one-off move is a `dose_events` row keyed on the slot it **came from**, status `deferred`, with the new time in `rescheduled_to` | `deferred` already means "put off to a later time, and still expected", which is exactly what a moved dose is. Keying on the original slot makes the move idempotent and reversible — a second move corrects the first instead of stacking rows |
| 06:14 | An **answered dose cannot be moved**, in the UI and at the endpoint | A confirmed dose is a record of what happened. Silently rewriting its time would falsify that rather than reschedule anything |
| 06:16 | `slotsForDay` applies reschedules, including across days, and never draws a moved occurrence twice | Drawing it at both the old and new time would also make the count of what is due wrong on both days — and that count is what the week strip and the day tally are built from |
| 06:25 | ⚠️ **Calendar drag-to-reschedule ships mouse-only** — no keyboard path, no touch. Flagged and deferred, not fixed | HTML5 drag-and-drop does not fire on a touchscreen, so on a phone the `2f` gesture does nothing. Accepted for now because nothing could be rescheduled at all before and the medicine editor is still a full accessible path to every time change — no capability is drag-only. The fix is a per-dose Move control calling the same `POST /app/doses/move` and `postMedications` the drop already calls, so it is a second way in rather than new behaviour |
| 06:40 | Rebased onto `origin/main` — 60 incoming commits, 11 replayed, **zero conflicts**. Both sides' work was disjoint (`agent/` vs `app/`+`api/`); only `.gitignore` overlapped and git auto-merged it | Rebase over merge per the single-`main` convention. Verified in a throwaway worktree before touching the real tree |
| 06:45 | **Two schemas for the same domain found on `main`.** Founder's call: **TRD §3 names win, and the scheduler's columns fold into them** | `agent/`'s model was cut for the dialler, `api/schema.sql` for the record and the consent chain, and neither was a superset. TRD §3 is what both lanes were meant to build against, so it is the tie-break that does not privilege whoever committed last |
| 06:48 | Adopted from the agent's model: `medications.start_date`, `dose_events.attempt_count`, `next_attempt_at`, `actor`, and the `pending` status | Each is load-bearing for dialling and had no equivalent here — without `start_date` a taper cannot be expressed, and without the retry pair the scheduler cannot avoid double-dialling. `pending` is bookkeeping, not an outcome: the app reads it exactly as it reads no row at all |
| 06:50 | `db._migrate()` added — new columns are ALTERed onto an existing database | `CREATE TABLE IF NOT EXISTS` silently skips a table that already exists, so a column added later would never appear on a developer's older file and every query naming it would fail |
| 06:52 | ~~`agent/` deliberately not modified~~ — **superseded 07:30**, see below | Held for about forty minutes, until the founder chose Route 2 |
| 07:30 | **Route 2 taken: one schema, one database.** `agent/` now loads `api/schema.sql` at boot instead of declaring its own 213-line DDL, and both processes default to `api/voxikin.db` | The split cost the thing the product is *for*: a dose moved on the calendar did not change which call was placed. Verified end to end — app onboards, agent reads the schedule with its prescription provenance, agent records an outcome from a call, app returns it over HTTP |
| 07:34 | `caregiver_name`/`caregiver_phone` denormalised off `patients` into the `caregivers` table, joined and **aliased back to the same names** | One caregiver record instead of a copy per patient. Aliasing meant `inbound-context.js` and the two tests asserting on those fields did not change at all — the shape above the repository layer is identical |
| 07:38 | ⚠️ **Two of my own `NOT NULL` constraints relaxed**, including `medications.confirmed_by`, which I had argued for on the grounds that *"a nullable confirmed_by is a gate that defaults to open"* | It is not the gate. `POST /app/onboarding` refuses an unsigned schedule, and the dial policy's **first** check is `schedule_signed_off_at == null → skip`. The column only blocked rows that legitimately exist before confirmation — a seeded regimen, a prescription read but not reviewed. The guarantee is unchanged and pinned by tests on both sides |
| 07:40 | `patients.caregiver_id` and `patients.name` made nullable | They were `NOT NULL` while onboarding was the only thing creating patients. The agent resolves inbound callers, and a patient known by number but not yet by name is a real record |
| 07:44 | `ORDER BY created_at DESC, id DESC` → `rowid DESC` in three queries | Nothing in the agent's code assumed numeric ids, but that ordering quietly relied on the autoincrement to break ties inside a one-second timestamp. Random uuids do not order. Caught by their own test, which is the argument for having run their suite at every step |
| 07:46 | **693 tests green across all three suites** — 588 `agent/`, 78 `api/`, 27 `app/`, from a 588-green baseline taken before anything was touched | The baseline is what made this safe to attempt on live calling code |
| 05:51 | **ElevenLabs added as a second call orchestrator**, behind the existing `TransportPort` seam. `active.transport: elevenlabs \| vapi` selects it; the Vapi path is unmodified | Spec `docs/superpowers/specs/2026-08-30-elevenlabs-outbound-transport-design.md`, plan `docs/superpowers/plans/2026-08-30-elevenlabs-outbound-transport.md`. Outbound only — inbound is deliberately unbuilt |
| 05:52 | ElevenLabs runs **its own LLM** (`gemini-2.5-flash`); our `/llm/chat/completions` is unused on this path | Founder's choice of approach C over a shared brain. Consequence recorded in the spec: the A/B compares whole stacks, not two voice pipelines against one brain, so a quality difference cannot be attributed to the voice layer alone |
| 05:53 | The prior product's agent is **duplicated, never patched**. Ours is `agent_7601m182ajm7eyqrpnfmee816d0g`; the source `agent_4901m0kzym5pfm7b7y9aprndv6qp` is untouched | It is English, carries its own 6,640-character prompt, and works. Rewriting it for a comparison we might not keep would destroy something real; the copy is also diffable against it |
| 05:54 | The tool webhook is **authenticated with a shared secret** (`X-Voxikin-Token`), failing closed when unset | Found by the implementer and the reviewer independently. The route is on a public tunnel and `report_outcome` accepts ESCALATED_SYMPTOM / ESCALATED_DISTRESS, which alert the patient's family — anyone who learned the URL could have raised a false medical alert, or forged CONFIRMED to mask a real missed dose |
| 05:55 | The agent config is **generated from the strategy files and re-pushed on every boot** | Tool URLs embed the ngrok origin and the free tier rotates it; the source agent had been pointing at a dead host for weeks with its tool calls failing silently. Generating rather than hand-maintaining also means a guardrail edit lands on both transports or neither — `SETUP.md` records that going wrong once |
| 05:56 | ⚠️ **Four of their API contracts were wrong in my plan and only a live call found them**: `execution_mode: 'sync'` is not a value (it is `immediate`/`post_tool_speech`/`async`); a tool property may set only ONE of five mutually-exclusive fields; there is no `post_call_webhook_url` (only a workspace webhook referenced by id); and the engine listens on `EVENT_TYPES.TOOL_CALLED`, not the `tool:<name>` channel I invented | ElevenLabs' prose docs specify none of these and their OpenAPI spec carries the enums without descriptions. Tests passed throughout because they asserted the shapes I had invented. Empirical verification against the real API is the only thing that caught any of it |
| 05:57 | The agent prompt is **templated with `{{placeholders}}`**, not interpolated at boot | The first real call greeted the patient as "रोहन जी" and asked about "Crocin" — the sample values from the use-case config, frozen into one script at boot. That, not prompt wording, is why the agent seemed vague and could not understand the answers |
| 05:58 | ⚠️ **Outstanding: the post-call webhook is not registered**, so call endings never reach the engine and outcomes do not persist | Creating it needs `webhooks_manage`, a workspace ROLE permission the founder's account lacks — not an API-key scope, so no key can be scoped around it. Must be created in the dashboard, or the role granted. `ELEVENLABS_POST_CALL_WEBHOOK_ID` is read on boot and the code is written and tested |
| 12:20 | **No payment gateway. FR-29 is a direct UPI transfer to our own VPA, confirmed by a human.** `api/payments/`, `payments` + `subscriptions` tables, `scripts/confirm_payment.py` | Razorpay and Cashfree both gate live mode behind merchant KYC — 1–2 working days with clean documents, 3–4 typically, and the top rejection cause is a bank-name/PAN mismatch nobody finds out about until day two. Code freeze was 5h40m away. Test-mode keys move no money and score nothing on Revenue L5, so the only route to a real rupee today was the one that needs no merchant onboarding at all. Razorpay KYC is still worth starting in parallel: the schema, the routes and the app are provider-neutral and only `api/payments/upi.py` swaps |
| 12:20 | **Every open order gets a unique paise suffix** — Care is issued at ₹499.01, ₹499.02, … and a partial unique index makes two open orders awaiting the same amount impossible (`payments_open_amount_unique`) | With no gateway there is no callback, so a person reconciles a bank statement by hand. Matching on the plan price alone would make two simultaneous buyers indistinguishable, and the resolution would be a guess — which is how somebody gets a month they did not pay for. The suffix makes the credit identify exactly one order. The UTR the buyer types back is corroboration and the idempotency key, not the primary match. Suffixes start at 1, never 0: a payment of exactly ₹499.00 is what someone sends when they round the amount, and that case must stay unattributable |
| 12:20 | **Confirmation has no HTTP route.** `scripts/confirm_payment.py --list` then `confirm_payment.py <utr> --by <name>`, run on the box | It is the step that grants a paid month, and this build has no operator authentication to put in front of an endpoint. A shell on the server is the authentication. The checkout screen tells the buyer in plain words that a person is checking, because that is what is actually happening — the alternative is a fake receipt, and on this product the whole posture is not lying to a worried user |
| 12:20 | **The landing page's "UPI checkout is not connected yet" line stays up until a real rupee has cleared** | The code merging is not the claim the sentence makes. It comes out when `evidence/payment/` has the credit and the `subscriptions` row, and not before |
| 12:20 | ⚠️ **Open: no gateway means no automatic renewal.** A month is granted once and lapses silently at `current_period_end` | Correct for a build with one real payment, wrong for a business. UPI Autopay or a gateway mandate is the post-hackathon fix, and it lands entirely inside `api/payments/` |
| 10:05 | ✅ **The post-call webhook was already registered** — the blocker above was two bugs of ours, not a permission | The route required our `X-Kinvox-Token`; ElevenLabs signs deliveries instead (`ElevenLabs-Signature`, HMAC over `${t}.${raw body}`). And it read `conversation_id` from the top level while the real payload nests everything under `data`. Both fixed; outcomes persist |
| 10:20 | The agent had no `end_call` tool, because our PATCH deleted it | Sending `tools` REPLACES the list, which silently dropped the source agent's system tools. The model responded to being told to call a missing tool by *saying* `[end_call]` aloud and never hanging up — the first real call ended only because the human gave up. System tools now go inside `tools`, established by probing the live API |
| 11:15 | `thinking_budget: 0` on `gemini-2.5-flash` | LLM p90 3255ms → 826ms. Also a safety fix: a v6 transcript has the model's deliberation spoken aloud to a patient in English, and reasoning that never happens cannot leak |
| 11:30 | `turn_eagerness: eager`, and a 180s call cap with a spoken closing line | Turn-taking became the largest remaining wait once the LLM was fixed. The cap is a bound outside the model, because the model cannot count its own turns and every prompt rule about looping half-worked. **If callers start being interrupted, set eagerness back to `normal` — do not edit the prompt** |
| 11:45 | Reminding a patient to take a prescribed dose is NOT medical advice, and the call must do it | Removing it satisfied an automated evaluator (33/100 → 100/100) and produced an agent that files a record and rings off. The evaluator measures compliance, not whether the call was worth making. Clinical *judgement* — amounts, schedule changes, starting or stopping — stays forbidden |
| 12:10 | On a refusal, ask once why and record the answer verbatim | "गोली खत्म हो गयी" and "पेट खराब हो जाता है" need completely different help from the family, and the call is the only place that reason is ever captured |
| 12:40 | The English prompt is refused at boot rather than served | It sat at v6 while Hindi reached v17, under a green parity test that matched guardrail *labels*. A quiet call carrying ten versions of missing safety work is worse than a loud failure |
| 13:10 | ⚠️ **Prescription reading was dead on `main`** and is now restored | The merged Analysing screen never called the extractor — it animated progress on timers and wrote a hardcoded medicine list into onboarding, whatever was uploaded. Caught only by an end-to-end test; the two assertions that would have caught it had been skipped as collateral of the UI merge |
| 13:30 | ⚠️ **Finishing onboarding signed the caregiver out** and is now fixed | `INSERT OR REPLACE` is DELETE-then-INSERT in SQLite, and `auth_sessions` cascades off `caregivers`. Nothing looked wrong afterwards — same id, updated name — which is why only walking the whole journey found it |
| 13:50 | Two call buttons, kept apart at every level | The demo returns a transcript and can touch nothing; the test call rings a real phone and its outcome is recorded. Separate routes, quotas, state and buttons, and the real one asks twice — a demo and a real call one flag apart is a misread away from ringing a patient |
| 14:45 | **Rebased onto `origin/main` after the `feat/app` merge; my own two commits dropped rather than replayed** | Both branches had independently made the same fix — reads and writes scoped to the session — and `feat/app` resolved the two-onboarding-handlers question the other way, keeping the snake_case handler in `api/caregiver/routes.py`. Replaying mine would have fought upstream at every hunk to arrive at the same behaviour under different names. Only the genuine delta was re-applied. The superseded work is on the local branch `backup/pre-rebase-1443` |
| 14:47 | ⚠️ **The merge left `main` red: 6 api tests and 8 agent tests failing.** All fixed | `test_end_to_end.py` came from `main` and posted camelCase; the handler it now reaches wants snake_case, so five of its tests failed on a 422 no source change had caused — the same app/API drift the merge was resolving, reintroduced in the test. The two sides were each internally consistent and the merge was not, which is the failure mode a merge cannot be trusted to surface on its own |
| 14:49 | ⚠️ **The Kinvox → Voxikin rename missed two occurrences**, both on the inbound path: the `/el/conversation-init` auth check and the webhook config it is paired with | They were consistent with *each other*, so production inbound still worked and only the tests — which had been renamed — failed. That is the dangerous shape: a half-applied rename that leaves a working system and a red suite looks like a flaky test. Both renamed to `X-Voxikin-Token`; the config is re-pushed on every boot, so the live agent picks it up |
| 14:51 | `findResumableSession` ordered by `id DESC` on a TEXT uuid; now `rowid DESC` | A fourth site missed by the 07:44 change. Two sessions ended in the same millisecond tie on `ended_at`, and random uuids do not order — so "the most recent dropped session wins" picked at random and failed about half the time. Confirmed flaky at 3 in 5 before, 8 of 8 green after |
| 14:53 | `test_only_one_onboarding_endpoint_is_registered` now walks nested routers via `original_router` | It iterated `app.routes` one level deep, but `include_router` leaves an `_IncludedRouter` wrapper rather than splicing children in — so it found neither handler and asserted `len([]) == 1`. It was failing for a reason unrelated to what it checks, and had it been written the other way round it would have *passed* over two handlers |
| 14:55 | `allow_credentials=True` on the CORS middleware, carried over from the dropped branch | Every `/app/*` endpoint reads the session cookie, and a `credentials: 'include'` fetch is blocked unless the response carries `Access-Control-Allow-Credentials`. Masked in development because `vite.config.ts` proxies `/auth` and `/app`, but `app/.env.local` points `VITE_API_BASE` straight at `:8000`, which does not go through that proxy |
| 14:57 | The app↔API onboarding shape is pinned by a test that reads `Consent.tsx` — now also checking `test_end_to_end.py`'s own literal | The Python tests post their own dict, so they were green throughout the original 422 and red for the wrong reason after the merge. Verified against the broken state: it names every drifted key |

| 15:10 | ⚠️ **The two call buttons could name a different patient than the screens show.** All six patient lookups in `api/caregiver/routes.py` now order by `created_at DESC` before `LIMIT 1`, matching `current_patient()` | They were a bare `LIMIT 1` — whatever row SQLite reached first — while `/app/record` has always ordered. With two patients under one caregiver they disagreed, and the disagreement was invisible in the worst way: the button reads *"Call +91… now?"*, which is the single fact the caregiver is asked to check before a real telephone rings, and it could name a household that was not on screen |

| 15:25 | ⚠️ **`load_dotenv()` moved above the `api.*` imports.** `VOXIKIN_DB` set in `.env` had no effect | `api/db.py` resolves `DB_PATH` from the environment at import time, and `.env` was loaded four lines later — so the setting was accepted, documented in `.env.example`, and silently ignored, with the API opening a different database than the one configured. It failed in the worst way available: no error, just an app with no patient in it, which is exactly how the Calendar's two call buttons came to report `ready: false` |

| 15:08 | **First real call through the Calendar button.** Rang `+918104348262`, answered, ran in Hindi, outcome persisted as `ESCALATED_SYMPTOM` from transcript analysis | The caller tested the guardrail by claiming to have taken cocaine. The agent did not read it as a taken dose, told them to speak to a doctor immediately, and escalated — which is the behaviour wanted. `conv_4501m190fz01fz1s8be57nf1myx5` |
| 15:12 | ⚠️ **The agent told the caller it would inform the family, and no alert was delivered.** `alert_sent_at` is stamped, `alert_channel` is `none`, and no `escalations` row was written | This is the v17 defect — *"claiming to be contacting the family when no alert had been raised"* — returning by a different route. It is no longer prompt wording: the escalation had nowhere to go, because the replacement `.env` carries Resend and SMTP but no WhatsApp or Twilio credential. A promise made to a frightened patient that the system cannot keep is worse than saying nothing |
| 15:14 | `POST /api/call` returns `call.conversation_id ?? call.id` | It read only `call.id`, which the ElevenLabs response does not carry, so a call that connected and recorded an outcome reported `callId: undefined` — surfacing to the caregiver as `conversation_id: null`. A real conversation looked like a failure that had somehow returned 200, and the panel had nothing to link to |
| 15:16 | ⚠️ **`ELEVENLABS_API_KEY` is no longer on disk in any env file.** The running agent still holds it from before `.env` was replaced | Nothing fails until the agent restarts, at which point `createCall` throws `Missing env var` and every call — demo and real — stops. The demo transcript and the real call today both worked purely because that process has been up since before the file changed |

| 15:35 | **The escalation plugin now picks a transport the environment can actually send on**, and gained an email sender over Resend's HTTP API | It called `buildTelegramSender()` unconditionally; with no bot token that returns null, so every escalation took the "no transport" branch, recorded `channel: 'none'` and notified nobody. Resend was already credentialled and the agent already uses `fetch`, so email needed no new dependency. A Telegram token still wins when present, so an existing deployment cannot silently change channel |
| 15:37 | The email transport **throws on a recipient with no `@`** rather than posting it | `caregiver_contact` holds a phone number on some calls. Resend would accept it, return 200 and drop it — the same "accepted and ignored" failure the ElevenLabs work hit five times. Throwing puts it in the delivery tally and records `failed`, which is a fact somebody can act on |
| 15:39 | ⚠️ **Delivery is configured but not yet proven.** `RESEND_FROM` is `onboarding@resend.dev`, Resend's shared sandbox domain | On that domain only the account holder's own address is deliverable; anyone else is accepted and silently dropped. So the alert path is credentialled and the credential is valid (`GET /domains` → 200), but whether a message actually arrives is unverified until one is sent and seen. Do not record this as working on the strength of the configuration alone |

| 15:48 | **Agent restarted; it booted with the restored key and re-patched `agent_7601m182ajm7eyqrpnfmee816d0g`** | The restart was the thing the missing key would have broken, so doing it deliberately — with the key back — is what proves the recovery rather than assuming it. The prior product's `agent_4901m0kzym…` was not touched |
| 15:50 | ✅ **The alert channel is delivered, verified, not assumed.** `escalation_alert_sent`, `channel: email`, recorded against the call | Driven through the real plugin and the real transport with a stub repository, so no call row was fabricated. The first attempt **failed**, which is the whole reason for sending one: `ALERT_OPERATOR_CONTACT` had been set to `voxikin.ai@gmail.com` and Resend rejected it with a 403 naming the only deliverable address on the sandbox domain — `soumya0343@gmail.com`. Configuration alone would have read as working right up until a real escalation |
| 15:52 | The failed attempt recorded `channel: 'failed'` and logged `escalation_alert_failed`, not silence | Worth recording as a design that held: the distinction between *nobody was configured* (`none`), *we tried and could not* (`failed`) and *it went* (`email`) is exactly what was missing when the agent told a patient their family would be told |


---

## Still open — resolve and record here

| # | Open question | Owner | Blocking? |
|---|---|---|---|
| 1 | **V1 — what exactly is "the Hive stack"?** Proposed mitigation: point ElevenLabs' custom LLM endpoint at the Hive model router, so voice transport stays ElevenLabs and the reasoning layer runs on-stack | ALL | **Yes** — rule 01 lists another stack as a disqualifier |
| 2 | **V2** — is Emergent required or merely sponsored? | ALL | Costs ~6h if required and we assumed otherwise |
| 3 | **V3** — exact inbound caller-ID variable | A | No — the ask-for-number fallback is unconditional |
| 4 | **V4** — live per-minute rates for the economics table | D | No, but needed before the video |
| 5 | **V5** — dynamic variables at call start? | A | No — costs one round trip if unavailable |
| 6 | **The Impact number.** The rubric grades Impact by how far a metric moves (<5%=L2, 5–10%=L3, 10–30%=L4, >30%=L5). No delta is currently claimed | ALL | No, but it silently costs two levels |
| 7 | **Appointment reminders** — cut in `PRD` §14, sold in the Care+ tier in §15. Mark "coming soon" or delete from the tier | D | No, but a judge is likely to open the pricing page |
| 8 | ~~**Product name**~~ — **settled 30 Aug: Voxikin** | ALL | Closed |
| 9 | **Lane owners.** Four lanes are defined; the people are not | ALL | **Yes** — Lane A is the critical path |

---

## ⚠️ Unreconciled — settle before the submission note is final

Two statements in this log contradict each other. Recording what is **verifiable on disk**
so the founder can settle the wording; nobody else should edit the origin note.

**Verified 30 Aug 00:35, by direct filesystem check:**

| Check | Result |
|---|---|
| `~/Desktop/projects/sahay-archive/` exists | **Yes** — 15 MB, created 29 Aug 20:23 |
| Contents | `Archive 2/` and `Archive 2.zip` |
| `Archive 2/va/va/src/` | **35 source files** |
| `Archive 2/va/va/package.json` → `name` | `elderly-voice-agent` |
| `Archive 2/va/va/.env` | populated `SARVAM_API_KEY`, `GROQ_API_KEY`, `API_KEY` |
| Anything from it inside this repo | **No** — confirmed absent from every commit |

So the Aug 30 row's claim that the directory "does not exist on disk" is **incorrect**;
it was moved there at 20:23 on 29 Aug and is still there. The 20:23 row is accurate as written.

**What is genuinely unsettled is whose work it is.** `Archive 2/va/PRODUCT-BRIEF.md` names
its owner as **Anmol**, while `PRD.md` and `TRD.md` name the owner as **Shubh Sankalp Das**.
If the archive is a teammate's or a third party's prior work rather than the submitting
founder's, then "pre-product, no codebase" may be accurate *about the submitter* and the
two rows are not actually in conflict.

**Three things are true regardless of how that resolves, and none of them are at risk:**

1. No archive code, config, or data is in this repo, and none was opened or copied
2. Every prompt and every line of `api/` was written after kickoff
3. The archive sits outside the repo and cannot be swept in by `git add`

**Decide before submission:** whether the origin note says *"no codebase"* or
*"a prior codebase exists for a related use case; it was not used, opened, or shown."*
The handbook's rule is that **hiding the origin is an auto-disqualification** while a
flagged borderline case is cleared by a mentor — so the more disclosive wording carries
less risk if there is any doubt about attribution. This is the founder's call, not a
drafting decision.

**Unrelated but pending:** the live `SARVAM_API_KEY` and `GROQ_API_KEY` in
`Archive 2/va/va/.env` should be rotated whenever there is a spare minute.
