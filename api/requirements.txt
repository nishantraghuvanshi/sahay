# Pinned. An unpinned build that works tonight and breaks tomorrow morning is
# the one failure mode a 19h event cannot absorb (NFR-7 rotates keys, not versions).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
httpx==0.28.1
asyncpg==0.30.0
pytest==8.3.4
pytest-asyncio==0.25.0
