# Pinned. An unpinned build that works tonight and breaks tomorrow morning is
# the one failure mode a 19h event cannot absorb (NFR-7 rotates keys, not versions).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.0

# asyncpg is deliberately absent. The caregiver-auth modules that arrived with
# this merge were written against a Postgres pool and are ported onto SQLite here.
#
# Two drivers, one dialect. A fresh clone still runs on the stdlib sqlite3 module
# with nothing to provision; deployments set TURSO_DATABASE_URL and this client
# talks to the same schema over the network, because the free tiers this runs on
# cannot attach a persistent disk and the voice agent shares the database.
libsql==0.1.11

# Prescription reading (api/rx_extract).
# requests is the pipeline's HTTP client; python-multipart backs the /extract
# file upload. Pillow is deliberately absent — nothing in the module's import
# closure uses it. See api/rx_extract/README.md.
requests==2.34.2
python-multipart==0.0.32
