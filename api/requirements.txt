fastapi
uvicorn[standard]
pydantic
python-dotenv
httpx
pytest

# Prescription extraction (api/rx_extract, vendored Pipeline B).
# requests is the pipeline's HTTP client; python-multipart backs the /extract
# file upload. The handoff also lists pillow, but nothing in Pipeline B's import
# closure uses it — see api/rx_extract/VENDORED.md.
requests
python-multipart
